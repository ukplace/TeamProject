<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.itwillbs.mapper.MemberMapper">


	
	<select id="userCheck" resultType="com.itwillbs.domain.MemberDTO">
		select * from member where m_email=#{m_email} and m_pass=#{m_pass}
	</select>

	<insert id="insertMember">
		insert into member(m_email,m_pass,m_name,m_tel,m_birth,m_zip,m_address,m_detail_address,m_date) 
		values(#{m_email}, #{m_pass}, #{m_name}, #{m_tel}, #{m_birth}, #{m_zip}, #{m_address}, #{m_detail_address}, #{m_date})	
	</insert>
	
	<select id="getUser">
		select * from member where m_email=#{m_email}
	</select>
	
</mapper>