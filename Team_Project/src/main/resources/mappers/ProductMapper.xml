<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.itwillbs.mapper.ProductMapper">

	<insert id="insertProduct">
	insert into product( p_name, p_explain, p_category, p_small_category, p_img, p_size, p_color, p_price, p_stock, p_productImg)
				values(#{p_name}, #{p_explain},#{p_category},#{p_small_category},#{p_img},#{p_size},#{p_color},#{p_price},#{p_stock},#{p_productImg})
	</insert>
	
</mapper>